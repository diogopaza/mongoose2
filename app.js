var connect = require('./connect');
/*
var movieSchema = new connect.Schema({

    title:{type: String},
    rating:String,
    realeaseYear:Number,
    hasCreditCookie: Boolean
})

var Movie = connect.model('Movie', movieSchema);
*/